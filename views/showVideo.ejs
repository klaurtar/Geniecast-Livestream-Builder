<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap"
      rel="stylesheet"
    />
    <script
      src="https://kit.fontawesome.com/b884a4526c.js"
      crossorigin="anonymous"
    ></script>
    <link rel="stylesheet" href="/css/videoTemplate.css" />

    <% if (video.multipleBackground) {%>
      <style>
        #dynamically-generated-background {
          background: <%= video.multipleBackground %>;
        }
      </style>
      <% } %>
  </head>
  <body>
    <div class="video-app" data-slug="<%= video.slug %>">
      <% if (video.logoContainerBackgroundColor) {%>
        <div class="logo-container" style="background-color:<%= video.logoContainerBackgroundColor %>">
        <% } else { %>
      <div class="logo-container">
        <% } %>
        <img src="<%= video.logo %>" alt="Geniecast Logo" />
      </div>

 
        

      <% if (video.backgroundColor) { %>
      <div class="navbar" style="background-color: <%= video.backgroundColor %>">
        <% } else { %>
      <div class="navbar">
        <% } %>
      </div>
      <div class="video">
        <% if (video.hostedPlatform === 'Youtube') { %>
        <iframe
          width="560"
          height="315"
          src="<%= video.video %>"
          frameborder="0"
          allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture"
          allowfullscreen
        ></iframe>
        <% } else if (video.hostedPlatform === 'Vimeo') { %>
          <iframe src="<%= video.video %>" width="640" height="360" frameborder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
          <% } %>
      </div>
      <% if (video.backgroundColor) { %>
      <div class="footer" style="background-color: <%= video.backgroundColor %>">
        <% } else { %>
          <div class="footer">
            <% } %>
            
            <% if (video.multipleBackground) {%>
            <div class="multi-background" id="dynamically-generated-background">
          <% } else { %>
            <div class="multi-background">
            <% } %>
          <div class="geniecast-presents"><%= video.title %></div>
        </div>
      </div>
    </div>
  </body>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.4.2/gsap.min.js"></script>

  <script src="https://unpkg.com/ejs@3.1.3/ejs.min.js"></script>
  <script src="/js/template.js"></script>
  <script src="/js/Sidebar.js"></script>
  <script type="module" src="/js/app.js"></script>
  <!-- <script>
    gsap.from('.logo-container', {
      duration: 2,
      opacity: 0,
      delay: 3,
    });
    gsap.from('.multi-background', {
      duration: 2,
      x: -200,
      opacity: 0,
      delay: 0.7,
    });
    gsap.from('.video', {
      duration: 1,
      x: 200,
      opacity: 0,
      delay: 0.7,
    });
    gsap.from('.sidebar', {
      duration: 2,
      backgroundPosition: '200px 0px',
      y: 200,
      opacity: 0,
      delay: 1.7,
    });
  </script> -->
</html>
